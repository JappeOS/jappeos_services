name: jappeos_services
description: "A service system that allows easy access to the jappeos_core systemd service using D-Bus."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  collection: ^1.19.1
  dbus: ^0.7.11
  flutter:
    sdk: flutter
  logging: ^1.3.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0